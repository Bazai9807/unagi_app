# Передача проекта между Windows и MacBook

Обновлено: 20 сентября 2026.

## Развёртывание сервера и телефон

- По прямому запросу пользователя проект размещён в `/home/unagi_app` на новом сервере. Открываются https://81.200.152.37/platform и https://81.200.152.37/business. Подробнее — `docs/SERVER.md`.
- Ubuntu 26.04, Node.js 24.21.0, PostgreSQL, nginx; отдельная служба `unagi`, HTTPS Let's Encrypt для IP, таймер продления. Новые серверные аккаунты и данные, локальная база ноутбука не переносилась.
- Доступы только в `backend/.data/server-access.txt` на сервере и локально, файл игнорируется Git. Первый вход требует настройки 2FA. Пароли и ключи не копировать в документы/чаты.
- Исправлен первоначальный переход к настройке 2FA, который раньше пытался загрузить защищённый обзор; в `/auth/me` добавлен `totpRequired`. Настроено доверие только локальному nginx при `TRUST_LOOPBACK_PROXY=true`.
- Проверены 18 серверных тестов, внешние HTTPS-страницы, оба входа/выхода, Secure cookie, запрет доступа до 2FA, перезапуск службы. Каталог демонстрационный, live-заказы/платежи по-прежнему выключены; мобильный клиент ещё не использует сервер.
- Android 0.1.0-demo повторно собран, установлен и запущен на подключённом Samsung SM-S942B 20 сентября. Сборка успешна, ADB подтвердил установку и процесс приложения. iOS не собирался.

## Задача пользователя

Платформа для разных бизнесов общепита: отдельное приложение каждого бизнеса, внутри его бренды; общий backend Android/iOS и две административные панели. Первый бизнес — UNAGI. Разработка идёт с двух ноутбуков через общий GitHub: `Bazai9807/unagi_app`. Актуальные решения находятся в `docs/PLATFORM-REQUIREMENTS.md`.

## Backend, 20 сентября 2026

- `backend/`: Node.js 24+, Fastify, PostgreSQL; для локальной работы PGlite без Docker. Запуск и подробная матрица готовности — `backend/README.md`, API — `backend/API.md`.
- Реализованы кабинеты платформы/бизнеса, серверные роли и изоляция, сессии/CSRF/TOTP, настройки брендов/точек/оплат/бонусных лимитов, оформление с публикацией, тестовые заказы, отчёты/CSV, тарифы/счета/SMTP-очередь, аудит и шифрование доступов.
- 17/17 серверных тестов прошли: настоящая локальная БД и HTTP API, внешние сервисы моделируются. В браузере проверены вход платформы, создание тестового заказа и публикация оформления.
- Реальные операции iiko/iikoCard и платёжный процесс НЕ завершены. Адаптеры звонка/SMS.RU, read-only iiko и криптография Т-Банка не означают готовую интеграцию. Live-заказы заблокированы; мобильный клиент ещё не связан с API.
- Следующие задачи: нормализация реального меню и модификаторов; жизненный цикл заказа iiko с восстановлением неизвестного результата; iikoCard; полный эквайринг с уведомлениями/сверкой/возвратами; единый мобильный API-клиент; размещение и эксплуатация.
- Провайдер входа звонком не выбран. Предложен SMS.RU Callcheck; альтернативы REDSMS Wait Call и LoginBot. Нужен исходящий звонок покупателя, не flash-call.
- Пользователь предоставил сервер, размещение описано выше; собственный домен пока не подключён. Секреты только в локальном окружении/защищённой настройке.
- На другом ноутбуке `cd backend && npm ci && npm run setup && npm start`. Базы и пароли локальные и разные; `.env`, `.data`, `.local-access.txt` в Git не попадают.

## Что уже работает

- Android-приложение `ru.unagi.mobile`, версия `0.1.0-demo`, min SDK 26 / target SDK 36.
- Java Activity + локальный WebView; без интернет-разрешения, внешних скриптов и JS bridge.
- 14 демо-позиций, категории, поиск, иллюстрации SVG, карточки, аллергены и избранное.
- Корзина, доставка/самовывоз, приборы, поля оформления, тестовый заказ, история и повтор заказа.
- Данные корзины, избранного и последних 50 заказов хранятся локально. Это не общий аккаунт и не синхронизация заказов между телефонами.
- Имя, телефон и адрес используются только при проверке формы и не записываются в историю.
- APK установлен и запускался на Samsung SM-S942B, Android 16, 15 сентября 2026.

## Решения, которые важно сохранить

- Первоначальная просьба о демо-меню сохраняется для установленной мобильной версии. Новая просьба разрешает разработку полной серверной интеграции, но доступы iiko ещё не предоставлены.
- Нет API-ключей и настоящих параметров заведения/доставки. Серверный прототип уже есть; реальная оплата и приём заказов пока не работают.
- Все условия примерные: минимальная доставка 700 ₽, доставка 199 ₽, бесплатно от 1 500 ₽, самовывоз без минимума.
- Статус тестового заказа не имитирует ресторан. Пометки «демо» обязательны до подключения настоящей интеграции.
- Весь общий клиентский код перенесён в `shared/www/`; Android Gradle включает `shared/` как assets, поэтому путь внутри APK остался `www/` и локальные данные не меняют origin.

## Следующая задача на MacBook

Создать iOS-оболочку в `ios/`, используя те же `shared/www/`. Предпочтительный путь для существующей архитектуры — WKWebView с локальными ресурсами. Создание Xcode-проекта, выбор Team, подпись и проверка на iPhone ещё не выполнены.

Проверить при реализации:

1. Автоматическое включение общего UI в bundle при сборке без ручной второй копии.
2. Постоянное локальное хранилище и стабильный origin, CSP и запрет чужой навигации; не ослаблять ограничения Android ради iOS.
3. Доступность `crypto.randomUUID()` в выбранном локальном контексте WKWebView. При необходимости сделать общий безопасный генератор ID с проверкой возможностей, не платформенную копию логики заказа.
4. Safe area, клавиатура, поворот экрана, VoiceOver, прокрутка карточек, закрытие модальных окон.
5. Полный сценарий тестового заказа и сохранение данных после перезапуска.
6. Настоящие возможности и ограничения проверить на iOS; не объявлять оболочку готовой только по сходству с Android.

## Проверки и артефакты

- `npm test`: 5 тестов бизнес-логики. Исторический результат первичной сборки — `artifacts/VERIFICATION.md`.
- `artifacts/unagi-demo.apk` и SHA256 — установочная демо-сборка от 15 сентября. При изменении кода не считать её автоматически обновлённой.
- После переноса в `shared/www` 20 сентября 2026: 5/5 тестов прошли; `assembleDebug lintDebug` завершились успешно. Все 6 ресурсов `assets/www/` внутри APK совпали по SHA256 с общими исходниками. Перенос не менял функциональность приложения.

## Как продолжить на втором ноутбуке

1. Клонировать репозиторий и открыть его корень как проект.
2. Читать `AGENTS.md`, этот файл и README.
3. Перед работой получить последние коммиты; создать ветку задачи.
4. После проверки записать изменения/ограничения сюда и отправить коммиты в GitHub.
5. На другом ноутбуке получить и объединить изменения перед следующей работой.

Пример первого запроса на Mac:

> Прочитай AGENTS.md, README.md и docs/HANDOFF.md. Создай iOS-приложение UNAGI в ios/ на основе общего shared/www. Сохрани одинаковую логику и дизайн Android/iOS; не дублируй общий код. iiko пока не подключаем. Собери и проверь iOS доступными средствами, обнови инструкции и отправь изменения в GitHub.
